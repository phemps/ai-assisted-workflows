{{ header }}
// Kind: storage-adapter
// Stack: cloudflare-r2

export class ArtifactStorage {
  constructor(env) {
    this.env = env;
  }
  async put(key, data) {
    // BEGIN-IMPL (keep)
  // TODO: Base
  // - ADVICE:R2:BASE: Put object to R2 (env.ARTIFACTS); set retention/ttl per policy
  // - ADVICE:R2:BASE: Avoid storing PII; consider encryption if needed
    // END-IMPL
  }
  async get(key) {
    // BEGIN-IMPL (keep)
  // TODO: Base
  // - ADVICE:R2:BASE: Get object with range support; handle 404
    // END-IMPL
  }
}
